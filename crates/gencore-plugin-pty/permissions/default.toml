"$schema" = "schemas/schema.json"

[default]
description = "No commands are enabled by default for the gencore-pty plugin. Consumers must explicitly allow the session, io, and resize commands they need."
permissions = []

"$schema" = "schemas/schema.json"

[default]
description = "Default permissions for the gencore-core plugin. Allows reading application metadata via `get_app_info`."
permissions = ["allow-get-app-info"]

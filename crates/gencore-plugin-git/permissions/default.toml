"$schema" = "schemas/schema.json"

[default]
description = "No commands are enabled by default for the gencore-git plugin. Consumers must explicitly allow the git commands they need."
permissions = []

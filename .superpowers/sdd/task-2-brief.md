# Task 2 / Wave 2 — Shared crates

Work from: `c:\Storage\Development\Workspace\Cursor\GenCore`

Wave 1 is done. Root `Cargo.toml` has `members = []` and workspace.dependencies:
- tauri 2.11.5, tauri-build 2.6.3, serde 1.0.229 (derive), serde_json 1.0.151, thiserror 2.0.20

## Goal

Implement three modular Tauri 2 plugins as workspace members. Template apps do not exist yet. Stubs only — no real PTY or filesystem.

## Cargo workspace

Update root `Cargo.toml` `members` to:
- `crates/gencore-core`
- `crates/gencore-plugin-pty`
- `crates/gencore-plugin-fs`

Use workspace.dependencies. Add `tauri-plugin` (build crate) as a workspace or per-crate build-dep at latest stable compatible with Tauri 2.11.

## Modular Rust layout (every crate)

```
src/lib.rs
src/modules/{module}/mod.rs
src/modules/{module}/{module}_api.rs
src/modules/{module}/{module}_error.rs
tests/   # integration tests only
build.rs
permissions/  # default.toml; autogenerated allow/deny via build.rs
```

No `#[cfg(test)]` modules in src unless a unit test cannot be an integration test.

## `crates/gencore-core` — plugin id `gencore-core`

Modules: `app_info`, `error`, `logging`

- `AppInfo { name: String, version: String, identifier: String }` — serde, deny_unknown_fields
- Async command `get_app_info` reads from `AppHandle` (`app.package_info()` / config identifier). No command args.
- `CoreError` typed (thiserror), never raw strings
- `logging` — optional tracing init helper (no-op if tracing not wanted; do not add heavy deps unless needed). Prefer no extra runtime deps.
- `build.rs`: `const COMMANDS: &[&str] = &["get_app_info"];` + `tauri_plugin::Builder::new(COMMANDS).build();`
- `permissions/default.toml`: default set includes `allow-get-app-info` only
- `pub fn init<R: Runtime>() -> TauriPlugin<R>`
- Tests in `crates/gencore-core/tests/app_info.rs` — at least serialize/shape of AppInfo and that CoreError is typed. Command can be tested as a unit of the API types if full Tauri harness is too heavy.

## `crates/gencore-plugin-pty` — plugin id `gencore-pty`

Modules: `session`, `io`, `resize`

Async stub commands: `open`, `write`, `resize`, `close`
- Validated serde args (`deny_unknown_fields`)
- Return structured `PtyError::NotImplemented` (or equivalent), never strings
- `build.rs` COMMANDS for those four
- `permissions/default.toml`: **empty default permissions** (no allow-*). Autogenerate allow/deny files only.
- Tests in `crates/gencore-plugin-pty/tests/` asserting stubs return NotImplemented

## `crates/gencore-plugin-fs` — plugin id `gencore-fs`

Do **not** name this `tauri-plugin-fs`.

Modules: `list`, `stat`, `watch`
Async stubs: `list`, `stat`, `watch` — same pattern as pty
Empty default permissions
Tests in `crates/gencore-plugin-fs/tests/`

## Constraints

- Latest stables only. Edition 2024.
- Secure: deny_unknown_fields, typed errors, async commands, ACL autogen.
- Do not create apps, ui-kit, .github, .husky, .vscode.
- Do not git commit.
- Verify: `cargo test --workspace` and `cargo clippy --workspace --all-targets -- -D warnings` and `cargo fmt --all -- --check`.

## Report

Write full report to `.superpowers/sdd/task-2-report.md`.

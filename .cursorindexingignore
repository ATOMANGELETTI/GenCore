# Build/generated output — keep out of the index, but still readable/editable on demand.
target/
dist/
.turbo/
**/src-tauri/gen/
**/src-tauri/target/

# Generated Tauri ACL schema files (regenerated by cargo/tauri build).
**/permissions/schemas/

# Lockfile noise — large and low-signal for semantic search.
pnpm-lock.yaml
Cargo.lock
